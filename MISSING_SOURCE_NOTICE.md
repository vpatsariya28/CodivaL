# Cannot apply requested website fixes in current repository state

I checked the repository contents and found that the project source files are missing.

Current repository only contains:
- `.git/` metadata
- `.gitkeep`

Because no frontend/backend application files are present, I cannot directly implement:
1. Contact form submission fix
2. Admin login fix
3. Careers profile submission fix
4. Creative image/content updates
5. "Whole website admin" panel location/update

## What to provide so I can complete your request quickly

Please push/provide the actual project files (for example: `package.json`, backend API files, database config, admin/auth routes, contact/careers form handlers, and frontend pages/components).

Once code is available, I will:
- Fix contact form API integration and validation.
- Fix admin authentication flow and credentials/session handling.
- Fix careers form upload/submission endpoint.
- Confirm where full-site admin controls are located (or add a dashboard if missing).
- Add creative images in homepage/services/careers sections.
- Share a screenshot of the updated UI.
